<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">Navbar</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav me-auto">
          <li class="nav-item">
            <a class="nav-link" href="{{ path('app_home') }}">Home</a>
          </li>
          {% if app.user.userIdentifier is defined %}
            <li class="nav-item">
              <a class="nav-link" href="{{ path('app_article_all') }}">Articles</a>
            </li>
              <a class="nav-link" href="{{ path('app_category_all') }}">Catégories</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a> {# Lien vers le formulaire de création d'un article #}
            </li>
          {% else %}
            <li class="nav-item">
              <a class="nav-link active" href="{{ path('app_register') }}">Inscription
                <span class="visually-hidden">(current)</span>
              </a>
            </li>
              <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
            </li>
          {% endif %}
      </div>
    </div>
</nav>