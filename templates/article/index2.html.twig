{% extends 'base.html.twig' %}

{% block title %}Liste Article{% endblock %}

{% block body %}
    <h1 class="text-center mt-5">Liste des articles</h1>
    <div class="d-grid w-25 mx-auto mt-4 mb-5">
        <a class="btn btn-primary" href="{{ path('app_article_add')}}" role="button">Ajouter un article</a>
    </div>
    {% for article in liste %}
        <div class="card mx-auto w-25 bg-light bg-gradient mt-5">
            <div class="card-header">
                <h5 class="card-title text-uppercase text-primary ">{{article.id}} - {{article.titre}}</h5>
            </div>
            
            <div class="card-body">
                <h6 class="card-subtitle mb-2 text-body-secondary">{{article.date | date ('d-m-Y')}}</h6>{#C'est un objet et pas un string, donc il faut changer sa MEF#}
                <h6 class="card-subtitle mb-2 text-body-secondary">{{article.user.nom | upper}} {{article.user.prenom}}</h6> {# user est un objet, donc il faut préciser quelle propriété on veut afficher (id, nom, prenom, mail...) UPPER est un filtre pour mettre en majuscule #}
                <p class="card-text">{{ article.contenu }}</p>
        
                <h6 class="card-subtitle mb-2 text-body-secondary"></h6><a href="{{ path('app_article_id', {'id':article.id}) }}">Voir l'article</a></h6> {# ici on créé un lien vers une route en utilisant la fonction path()=> permet de changer le nom ou le chemin de la route sans avoir besoin de l'actualiser ici #}
            </div>
        </div>
    {% endfor %}
{% endblock %}
